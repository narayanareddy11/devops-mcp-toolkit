apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: devops
  labels:
    app: vault
spec:
  type: NodePort
  selector:
    app: vault
  ports:
    - name: http
      port: 8200
      targetPort: 8200
      nodePort: 30200
